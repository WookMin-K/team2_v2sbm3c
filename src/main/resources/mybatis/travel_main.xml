<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.mvc.main_travel.TravelMainDAOInter">

    <!-- 여행 일정 등록 -->
    <insert id="create" parameterType="dev.mvc.main_travel.TravelMainVO">
        INSERT INTO CopyOfplan (
        plan_no, title, start_date, departure, transport, people_count,
        place, contentstype, x, y, trip_day, user_no
        )
        VALUES (
        copyofplan_seq.nextval, #{title}, #{start_date}, #{departure}, #{transport}, #{people_count},
        #{place}, #{contentstype}, #{x}, #{y}, #{trip_day}, #{user_no}
        )
    </insert>

    <!-- 특정 회원의 여행 일정 목록 조회 -->
    <select id="listByUserNo" parameterType="int" resultType="dev.mvc.main_travel.TravelMainVO">
        SELECT plan_no, title, start_date, departure, transport, people_count,
        place, contentstype, x, y, trip_day, user_no
        FROM CopyOfplan
        WHERE user_no = #{user_no}
        ORDER BY trip_day ASC, plan_no ASC
    </select>

    <!-- 특정 회원의 일정 모두 삭제 -->
    <delete id="deleteByUserNo" parameterType="int">
        DELETE FROM CopyOfplan
        WHERE user_no = #{user_no}
    </delete>

</mapper>
